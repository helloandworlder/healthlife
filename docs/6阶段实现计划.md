# HealthLife App 6阶段实现计划

## 总览

| Phase | 内容 | 预计时间 | 核心交付物 |
|-------|------|---------|-----------|
| Phase 1 | 项目基础架构搭建 | 5-7 天 | 技术框架、数据库、路由、共享组件 |
| Phase 2 | 用户Profile与健康数据源 | 5-7 天 | Onboarding、健康数据同步 |
| Phase 3 | 今天Dashboard与行动日历 | 7-10 天 | 今天Tab、行动Tab完整功能 |
| Phase 4 | 目标系统与宠物养成 | 7-10 天 | 目标Tab、宠物系统、通知 |
| Phase 5 | 回顾功能与AI聊天 | 5-7 天 | 周回顾、分享、AI聊天 |
| Phase 6 | 设置与优化发布 | 5-7 天 | 设置Tab、优化、测试、发布 |
| **总计** | - | **34-48 天** | - |

---

## Phase 1: 项目基础架构搭建 (5-7天)

### 1.1 项目初始化与依赖配置 (Day 1)

#### 任务清单
- [ ] 更新 `pubspec.yaml` 添加所有核心依赖
- [ ] 配置 `analysis_options.yaml` 代码规范
- [ ] 创建完整目录结构骨架
- [ ] 配置 build_runner 和代码生成脚本

#### pubspec.yaml 依赖列表
```yaml
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # 状态管理
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  
  # 路由
  go_router: ^14.2.0
  
  # 数据库
  drift: ^2.18.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.3
  path: ^1.9.0
  
  # 健康数据
  health: ^13.2.1
  
  # 通知
  flutter_local_notifications: ^18.0.0
  timezone: ^0.9.4
  
  # 网络
  dio: ^5.5.0
  
  # UI组件
  fl_chart: ^0.68.0
  lottie: ^3.1.2
  
  # 工具
  intl: ^0.19.0
  collection: ^1.18.0
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  share_plus: ^9.0.0
  
  # 错误监控
  sentry_flutter: ^8.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
  # 代码生成
  build_runner: ^2.4.11
  riverpod_generator: ^2.4.0
  freezed: ^2.5.2
  json_serializable: ^6.8.0
  drift_dev: ^2.18.0
  go_router_builder: ^2.7.0
```

#### 目录结构创建脚本
```bash
mkdir -p lib/{app,core/{theme,constants,extensions,utils},shared/{db/{tables,daos},services,repositories,widgets},features/{onboarding,profile,health_source,dashboard,meals,weight,activity_calendar,goals,gamification,review,chat_ai,settings}/{presentation/widgets,application,domain,data}}
```

---

### 1.2 核心模块搭建 (Day 2)

#### 1.2.1 主题系统 (`core/theme/`)
- [ ] **app_colors.dart**: 颜色常量定义
  - 主色调 (Primary)
  - 辅助色 (Secondary)
  - 背景色 (Background)
  - 文字色 (Text)
  - 状态色 (Success/Warning/Error)
  - 活动等级色 (0-3级热力图颜色)

- [ ] **app_text_styles.dart**: 文字样式定义
  - Display / Headline / Title / Body / Label 各级别
  - 中英文字体配置

- [ ] **app_theme.dart**: 主题配置
  - Light Theme
  - Dark Theme
  - 组件主题 (Card/Button/Input/AppBar等)

#### 1.2.2 常量定义 (`core/constants/`)
- [ ] **route_names.dart**: 路由名称常量
- [ ] **db_constants.dart**: 数据库表名、版本常量
- [ ] **health_types.dart**: 健康数据类型枚举

#### 1.2.3 工具类 (`core/utils/`)
- [ ] **date_utils.dart**: 日期处理工具
  - `DateTime.dateOnly` 扩展
  - `getWeekStart()` / `getWeekEnd()`
  - `formatDate()` 格式化方法
  
- [ ] **calorie_calculator.dart**: 热量计算
  - `calculateBMR()` - 基础代谢
  - `calculateTDEE()` - 每日总消耗
  - `calculateRemainingCalories()` - 剩余可摄入

- [ ] **bmi_calculator.dart**: BMI计算
  - `calculateBMI()`
  - `getBMICategory()`

---

### 1.3 数据库层实现 (Day 3-4)

#### 1.3.1 表结构定义 (`shared/db/tables/`)
- [ ] `user_profiles.dart` - 用户档案表
- [ ] `plans.dart` - 减重计划表
- [ ] `weight_records.dart` - 体重记录表
- [ ] `meal_logs.dart` - 饮食记录表
- [ ] `daily_metrics.dart` - 每日指标表
- [ ] `goals.dart` - 目标定义表
- [ ] `goal_logs.dart` - 目标完成记录表
- [ ] `pet_states.dart` - 宠物状态表
- [ ] `weekly_summaries.dart` - 每周回顾表
- [ ] `health_sync_states.dart` - 同步状态表
- [ ] `app_settings.dart` - 应用设置表

#### 1.3.2 数据库主类 (`shared/db/app_database.dart`)
```dart
@DriftDatabase(
  tables: [
    UserProfiles,
    Plans,
    WeightRecords,
    MealLogs,
    DailyMetrics,
    Goals,
    GoalLogs,
    PetStates,
    WeeklySummaries,
    HealthSyncStates,
    AppSettings,
  ],
  daos: [
    UserProfileDao,
    PlanDao,
    WeightDao,
    MealDao,
    MetricsDao,
    GoalDao,
    PetDao,
    WeeklySummaryDao,
    SettingsDao,
  ],
)
class AppDatabase extends _$AppDatabase {
  AppDatabase() : super(_openConnection());
  
  @override
  int get schemaVersion => 1;
  
  @override
  MigrationStrategy get migration => MigrationStrategy(
    onCreate: (m) async {
      await m.createAll();
      // 初始化默认设置
      await into(appSettings).insert(AppSettingsCompanion.insert(
        key: 'onboarding_completed',
        value: 'false',
      ));
    },
    onUpgrade: (m, from, to) async {
      // 未来版本迁移
    },
  );
}
```

#### 1.3.3 DAOs 实现 (`shared/db/daos/`)
- [ ] **UserProfileDao**
  - `getProfile()` / `upsertProfile()`
  
- [ ] **PlanDao**
  - `getActivePlan()` / `createPlan()` / `endPlan()`
  
- [ ] **WeightDao**
  - `getLatestWeight()` / `getWeightHistory()` / `insertWeight()`
  
- [ ] **MealDao**
  - `getMeals(date)` / `insertMeal()` / `deleteMeal()`
  
- [ ] **MetricsDao**
  - `getMetrics(date)` / `upsertMetrics()` / `getWeekMetrics()`
  
- [ ] **GoalDao**
  - `getAllGoals()` / `createGoal()` / `updateGoal()` / `deleteGoal()`
  
- [ ] **GoalLogDao** (合并到GoalDao)
  - `getLog(goalId, date)` / `incrementProgress()` / `decrementProgress()`
  
- [ ] **PetDao**
  - `getOrCreateState()` / `updateExp()` / `updateCompanionDays()`
  
- [ ] **SettingsDao**
  - `getSetting(key)` / `setSetting(key, value)` / `getAllSettings()`

#### 1.3.4 运行代码生成
```bash
dart run build_runner build --delete-conflicting-outputs
```

---

### 1.4 路由系统 (Day 5)

#### 1.4.1 GoRouter 配置 (`app/router.dart`)
- [ ] 定义所有路由路径常量
- [ ] 配置 ShellRoute (底部导航栏)
- [ ] 配置各 Tab 及其子路由
- [ ] 配置独立页面路由
- [ ] 实现 redirect 逻辑 (Onboarding检查)

#### 1.4.2 类型安全路由 (go_router_builder)
- [ ] 为每个路由创建 GoRouteData 类
- [ ] 运行 build_runner 生成类型安全导航方法

#### 1.4.3 Deep Linking 配置
- [ ] iOS: Universal Links 配置 (预留)
- [ ] Android: App Links 配置 (预留)

---

### 1.5 状态管理基础 (Day 5)

#### 1.5.1 全局 Provider (`app/providers.dart`)
- [ ] `databaseProvider` - 数据库单例
- [ ] `notificationServiceProvider` - 通知服务
- [ ] `healthSourceProvider` - 健康数据源 (平台判断)
- [ ] `currentProfileProvider` - 当前用户档案
- [ ] `activePlanProvider` - 当前激活计划
- [ ] `petStateProvider` - 宠物状态
- [ ] `settingsProvider` - 应用设置

---

### 1.6 共享组件 (Day 6-7)

#### 1.6.1 页面框架组件
- [ ] **AppScaffold**: 统一页面框架
  - 支持 AppBar 配置
  - 支持 FAB
  - 支持 Bottom Sheet

- [ ] **BottomNavBar**: 底部导航栏
  - 4个Tab图标和标签
  - 选中状态高亮
  - 点击切换路由

#### 1.6.2 通用UI组件
- [ ] **SectionCard**: 卡片组件
  - 标题 + 内容布局
  - 圆角 + 阴影样式
  - 点击事件支持

- [ ] **LoadingState**: 加载状态组件
  - 居中转圈
  - 可选加载文字

- [ ] **EmptyState**: 空状态组件
  - 图标 + 文字 + 可选按钮

- [ ] **ErrorState**: 错误状态组件
  - 错误图标 + 消息 + 重试按钮

#### 1.6.3 App入口 (`app/app.dart`)
- [ ] MaterialApp.router 配置
- [ ] ProviderScope 包装
- [ ] 主题配置
- [ ] 错误处理

#### 1.6.4 main.dart
- [ ] 初始化 Flutter Binding
- [ ] 初始化通知服务
- [ ] 初始化 Sentry (可选)
- [ ] 运行 App

---

### Phase 1 验收标准
- [ ] 项目可以正常编译运行
- [ ] 数据库表创建成功
- [ ] 底部导航栏可以切换 (空白页面)
- [ ] 主题切换正常
- [ ] 代码生成无报错

---

## Phase 2: 用户Profile与健康数据源 (5-7天)

### 2.1 首次启动引导 (Day 1-2)

#### 2.1.1 Onboarding 页面设计
- [ ] **OnboardingPage**: 主页面容器
  - PageView 横向滑动
  - 底部进度指示器
  - 下一步/完成按钮

- [ ] **Step 1 - WelcomePage**: 欢迎页
  - App Logo
  - 欢迎语
  - 功能介绍 (3-4个亮点)

- [ ] **Step 2 - ProfileInputPage**: 个人信息录入
  - 性别选择 (SegmentedButton)
  - 身高输入 (滑动选择器或输入框)
  - 体重输入 (滑动选择器或输入框)
  - 出生日期选择 (DatePicker)
  - 单位切换 (kg/lb, cm/inch)

- [ ] **Step 3 - ActivityLevelPage**: 活动水平选择
  - 4个选项卡片
    - 久坐 (sedentary): 办公室工作，很少运动
    - 轻度活动 (light): 每周运动1-3次
    - 中度活动 (moderate): 每周运动3-5次
    - 高度活动 (active): 每天运动或体力劳动
  - 每个选项配图和说明

- [ ] **Step 4 - HealthSourcePage**: 健康数据源连接
  - 说明数据同步的好处
  - Apple Health / Health Connect 连接按钮
  - 跳过选项

#### 2.1.2 Onboarding 状态管理
- [ ] **OnboardingNotifier**
  - 当前步骤状态
  - 表单数据收集
  - 表单验证
  - 完成处理

- [ ] **CompleteOnboardingUseCase**
  - 保存用户档案到数据库
  - 更新 onboarding_completed 设置
  - 初始化宠物状态
  - 跳转到主页

---

### 2.2 健康数据源模块 (Day 3-4)

#### 2.2.1 接口定义 (`features/health_source/domain/`)
- [ ] **HealthMetricType** 枚举
- [ ] **HealthSample** 数据类
- [ ] **HealthSourceRepository** 抽象接口

#### 2.2.2 iOS 实现 (`shared/repositories/apple_health_repository.dart`)
- [ ] 使用 `health` 插件封装 HealthKit
- [ ] `isAvailable()` - 检查 HealthKit 可用性
- [ ] `requestPermissions()` - 请求权限
  - 读取: 步数、心率、HRV、睡眠、体重、活动能量
  - 写入: 体重 (可选)
- [ ] `fetchSamples()` - 获取数据样本
- [ ] 数据类型映射表

#### 2.2.3 Android 实现 (`shared/repositories/health_connect_repository.dart`)
- [ ] 使用 `health` 插件封装 Health Connect
- [ ] `isAvailable()` - 检查 Health Connect 安装
- [ ] `requestPermissions()` - 请求权限
- [ ] `fetchSamples()` - 获取数据样本
- [ ] 引导用户安装 Health Connect (如未安装)

#### 2.2.4 平台配置
- [ ] **iOS Info.plist**
  - NSHealthShareUsageDescription
  - NSHealthUpdateUsageDescription
  
- [ ] **Android AndroidManifest.xml**
  - Health Connect 权限声明
  - Intent Filter

---

### 2.3 健康数据同步服务 (Day 5)

#### 2.3.1 同步服务 (`shared/services/health_sync_service.dart`)
- [ ] **HealthSyncService**
  - `syncAll()` - 同步所有数据类型
  - `syncType(type)` - 同步指定类型
  - `getLastSyncTime(type)` - 获取上次同步时间
  - `updateLastSyncTime(type)` - 更新同步时间

#### 2.3.2 同步用例 (`features/health_source/application/sync_health_data_usecase.dart`)
```dart
class SyncHealthDataUseCase {
  Future<SyncResult> call() async {
    final results = <HealthMetricType, int>{};
    
    for (final type in HealthMetricType.values) {
      final lastSync = await syncStateRepo.getLastSyncTime(type);
      final from = lastSync ?? DateTime.now().subtract(Duration(days: 30));
      final to = DateTime.now();
      
      final samples = await healthRepo.fetchSamples(type: type, from: from, to: to);
      
      // 按日聚合并写入 DailyMetrics
      final processed = await metricsRepo.ingestSamples(type, samples);
      results[type] = processed;
      
      await syncStateRepo.updateLastSyncTime(type, to);
    }
    
    return SyncResult(results);
  }
}
```

#### 2.3.3 数据聚合逻辑
- [ ] 步数: 按日求和
- [ ] 活动分钟: 按日求和
- [ ] 消耗热量: 按日求和
- [ ] 睡眠: 按日计算总时长
- [ ] 心率: 按日计算平均值
- [ ] HRV: 按日计算平均值
- [ ] 体重: 写入 WeightRecords 表

---

### 2.4 个人信息管理页面 (Day 6-7)

#### 2.4.1 个人信息页 (`features/profile/presentation/profile_page.dart`)
- [ ] 显示当前档案信息
- [ ] 编辑各字段
- [ ] 保存更新

#### 2.4.2 数据源管理页 (`features/health_source/presentation/health_source_page.dart`)
- [ ] 显示当前连接状态
- [ ] 重新连接/断开连接
- [ ] 手动触发同步
- [ ] 显示上次同步时间

#### 2.4.3 BMR/TDEE 计算展示
- [ ] 根据用户档案计算 BMR
- [ ] 根据活动水平计算 TDEE
- [ ] 在个人信息页展示

---

### Phase 2 验收标准
- [ ] 首次启动显示引导流程
- [ ] 可以完成档案填写
- [ ] iOS 可连接 Apple Health
- [ ] Android 可连接 Health Connect
- [ ] 健康数据可以同步到本地数据库
- [ ] 引导完成后进入主页

---

## Phase 3: 今天Dashboard与行动日历 (7-10天)

### 3.1 今天Tab - 计划概览卡 (Day 1-2)

#### 3.1.1 计划概览卡组件
- [ ] **PlanOverviewCard**
  - 无计划状态: 显示"开始新计划"按钮
  - 有计划状态:
    - 计划进度: 第N天/总天数
    - 体重仪表盘
    - 计划日期范围

- [ ] **WeightGauge** 圆弧仪表盘组件
  - 圆弧背景
  - 进度弧线 (初始体重到当前体重)
  - 居中显示当前体重
  - 下方显示 BMI 值
  - 左右标签: 初始体重 / 目标体重

#### 3.1.2 创建计划功能
- [ ] **CreatePlanSheet** (Bottom Sheet)
  - 初始体重输入 (默认当前体重)
  - 目标体重输入
  - 计划时长选择: 30/60/90天或自定义
  - 每日热量缺口设置 (默认500kcal)
  - 预估文案: "按此计划，预计每周减重X公斤"

- [ ] **CreatePlanUseCase**
  - 计算开始/结束日期
  - 保存到 Plans 表
  - 刷新 Dashboard

---

### 3.2 今天Tab - 卡路里卡片 (Day 3-4)

#### 3.2.1 卡路里卡片组件
- [ ] **CalorieCard**
  - 环形进度图 (已摄入/可摄入)
  - 大数字: "还能吃 XXX 千卡"
  - 详情行: 消耗XXX - 饮食YYY - 缺口ZZZ
  - 超标提示: "今日已超出目标 XXX 千卡"

- [ ] **MealEntryButtons** 四餐入口
  - 早餐 / 午餐 / 晚餐 / 加餐
  - 每个显示已记录热量
  - 点击进入添加饮食

#### 3.2.2 热量计算用例
- [ ] **CalculateCalorieBudgetUseCase**
```dart
CalorieBudget call() {
  final burned = todayMetrics.caloriesBurned;
  // 如果没有同步数据，使用预估的TDEE
  final burnedOrEstimate = burned > 0 ? burned : estimatedTDEE;
  
  final intake = todayMeals.sum((m) => m.calories);
  final deficit = activePlan?.dailyCalorieDeficit ?? 0;
  
  final remaining = max(0, burnedOrEstimate - intake - deficit);
  
  return CalorieBudget(
    burned: burnedOrEstimate,
    intake: intake,
    deficit: deficit,
    remaining: remaining,
  );
}
```

---

### 3.3 饮食记录模块 (Day 4-5)

#### 3.3.1 添加饮食页面
- [ ] **AddMealSheet** (Bottom Sheet)
  - 餐次选择 (如果从入口进入则预选)
  - 搜索/输入食物名称
  - 热量输入 (kcal)
  - 常见食物模板快速选择

- [ ] **FoodTemplates** 常见食物模板数据
```dart
const foodTemplates = {
  'breakfast': [
    FoodTemplate('包子 (1个)', 220),
    FoodTemplate('鸡蛋 (1个)', 78),
    FoodTemplate('牛奶 (250ml)', 135),
    FoodTemplate('燕麦粥', 150),
    FoodTemplate('豆浆 (300ml)', 90),
  ],
  'lunch': [
    FoodTemplate('米饭 (1碗)', 230),
    FoodTemplate('面条 (1碗)', 280),
    FoodTemplate('炒菜 (1份)', 150),
    FoodTemplate('红烧肉', 350),
  ],
  // ...
};
```

#### 3.3.2 饮食记录用例
- [ ] **LogMealUseCase**
  - 保存到 MealLogs 表
  - 刷新 Dashboard 卡路里

- [ ] **GetTodayMealsUseCase**
  - 获取今日所有饮食记录
  - 按餐次分组

---

### 3.4 今天Tab - 体重卡片 (Day 5-6)

#### 3.4.1 体重卡片组件
- [ ] **WeightCard**
  - 今日体重 (如有记录)
  - 与前一次差值: +0.2kg / -0.5kg
  - "+" 快速记录按钮
  - 点击进入历史页

- [ ] **AddWeightSheet** (Bottom Sheet)
  - 体重数字输入
  - 当前日期时间
  - 保存按钮

#### 3.4.2 体重历史页面
- [ ] **WeightHistoryPage**
  - 时间范围选择: 7天 / 30天 / 90天
  - 折线图 (fl_chart)
    - X轴: 日期
    - Y轴: 体重
    - 每天一个数据点
  - 统计信息:
    - 最高/最低/平均体重
    - 总变化量
  - 体重记录列表

---

### 3.5 行动Tab - 周视图 (Day 7-8)

#### 3.5.1 周视图组件
- [ ] **WeekView**
  - 日期范围标题: "11月9日 - 11月15日"
  - 连胜状态: "🔥 连续达标 5 天" 或 "连胜中断"
  - 本周统计卡片:
    - 运动达标天数
    - 总运动分钟
    - 总消耗千卡

- [ ] **WeekDayRow** 周日历行
  - 周日 ~ 周六 7天
  - 每天显示:
    - 日期数字
    - 活动状态圆点 (颜色深浅表示活动量)
  - 当前日期高亮
  - 点击某天查看详情

#### 3.5.2 周数据用例
- [ ] **GetWeekStatsUseCase**
```dart
WeekStats call(DateTime weekStart) {
  final metrics = metricsRepo.getMetrics(weekStart, weekStart.add(Duration(days: 6)));
  
  return WeekStats(
    activeDays: metrics.where((m) => m.activeMinutes >= 30).length,
    totalMinutes: metrics.sum((m) => m.activeMinutes),
    totalCalories: metrics.sum((m) => m.caloriesBurned),
    streak: calculateStreak(metrics),
    dayActivities: metrics.map((m) => DayActivity(
      date: m.date,
      level: getActivityLevel(m),
    )).toList(),
  );
}
```

---

### 3.6 行动Tab - 月视图 (Day 8-9)

#### 3.6.1 月视图组件
- [ ] **MonthView**
  - 月份选择器 (左右箭头切换)
  - 日历网格 (7列 x 5-6行)
  - 每天小方块/圆点
  - 颜色表示活动等级:
    - 0级 (灰色): 无活动
    - 1级 (浅绿): 轻度活动
    - 2级 (中绿): 中度活动
    - 3级 (深绿): 高强度活动

#### 3.6.2 活动等级计算
```dart
ActivityLevel getActivityLevel(DailyMetric m) {
  final score = m.steps / 10000 + m.activeMinutes / 60;
  if (score < 0.3) return ActivityLevel.none;
  if (score < 0.6) return ActivityLevel.light;
  if (score < 1.0) return ActivityLevel.moderate;
  return ActivityLevel.high;
}
```

---

### 3.7 当日详情页 (Day 9-10)

#### 3.7.1 当日详情组件
- [ ] **DayDetailPage**
  - 日期标题
  - 步数卡片 (大数字 + 目标进度)
  - 运动分钟卡片
  - 消耗热量卡片
  - 睡眠时长卡片
  - 心率/HRV卡片 (如有数据)
  - "查看当日回顾" 入口

---

### Phase 3 验收标准
- [ ] 今天Tab显示计划概览卡
- [ ] 可以创建减重计划
- [ ] 卡路里卡片显示正确计算结果
- [ ] 可以添加饮食记录
- [ ] 可以记录体重并查看历史
- [ ] 行动Tab周视图和月视图正常显示
- [ ] 点击日期可查看当日详情

---

## Phase 4: 目标系统与宠物养成 (7-10天)

### 4.1 目标Tab - 宠物头部 (Day 1-2)

#### 4.1.1 宠物头部组件
- [ ] **PetHeader**
  - 宠物头像/动画 (Lottie/Rive)
  - 宠物名字: "TATA"
  - 陪伴天数: "陪伴 3 天"
  - 等级进度条:
    - Lv.3
    - 经验条 (当前120/300)
  - 点击进入宠物详情

#### 4.1.2 陪伴天数逻辑
- [ ] **UpdateCompanionDaysUseCase**
  - 每日首次打开检查
  - 如果 lastActiveDate != today，companionDays++
  - 更新 lastActiveDate

---

### 4.2 目标Tab - 目标看板 (Day 2-3)

#### 4.2.1 目标卡片列表
- [ ] **GoalCard**
  - 图标 + 目标名称
  - 进度条: 今日完成2/3次
  - 打卡按钮 (勾选图标)
  - 长按显示编辑/删除菜单

#### 4.2.2 目标类型展示
- [ ] **每日型目标**
  - 显示: "今日 2/3 次"
  - 打卡增加次数

- [ ] **每周型目标**
  - 显示: "本周 3/5 天"
  - 每天最多打卡1次

- [ ] **时长型目标** (自动完成)
  - 如: 睡眠 ≥ 8小时
  - 根据同步数据自动判断完成

---

### 4.3 创建目标流程 (Day 3-4)

#### 4.3.1 创建目标入口
- [ ] 底部浮动 "+" 按钮
- [ ] **CreateGoalSheet** (Bottom Sheet)
  - 分类 Tab: 推荐 / 改善睡眠 / 生活习惯
  - 推荐目标模板列表
  - "创建自定义目标" 入口

#### 4.3.2 目标模板数据
```dart
const goalTemplates = [
  GoalTemplate(
    name: '坚持吃早餐',
    icon: '🍳',
    period: GoalPeriod.daily,
    target: 1,
    category: 'recommend',
  ),
  GoalTemplate(
    name: '喝八杯水',
    icon: '💧',
    period: GoalPeriod.daily,
    target: 8,
    category: 'recommend',
  ),
  GoalTemplate(
    name: '每周运动3次',
    icon: '🏃',
    period: GoalPeriod.weekly,
    target: 3,
    category: 'recommend',
  ),
  GoalTemplate(
    name: '睡前不玩手机',
    icon: '📵',
    period: GoalPeriod.daily,
    target: 1,
    category: 'sleep',
  ),
  GoalTemplate(
    name: '睡够8小时',
    icon: '😴',
    period: GoalPeriod.daily,
    target: 1,
    category: 'sleep',
  ),
  // ...
];
```

#### 4.3.3 目标编辑页
- [ ] **GoalEditPage**
  - 图标选择器 (emoji grid)
  - 名称输入框
  - 周期选择: 每日 / 每周
  - 频率输入:
    - 每日: 每天 N 次
    - 每周: 每周 N 天
  - 提醒开关
  - 提醒时间选择 (TimePicker)
  - 保存/取消按钮

---

### 4.4 打卡与经验值 (Day 5-6)

#### 4.4.1 打卡用例
- [ ] **ToggleGoalCompletionUseCase**
```dart
Future<ToggleResult> call(int goalId) async {
  final goal = await goalRepo.getGoal(goalId);
  final log = await goalLogRepo.getOrCreateLog(goalId, today);
  
  if (log.progress < goal.target) {
    // 打卡 +1
    await goalLogRepo.incrementProgress(goalId, today);
    
    // 检查是否刚好完成
    if (log.progress + 1 >= goal.target) {
      // 奖励经验
      await gamificationService.awardExp(expPerGoal);
      return ToggleResult.completed;
    }
    return ToggleResult.incremented;
  } else {
    // 已完成，点击则取消最后一次
    await goalLogRepo.decrementProgress(goalId, today);
    // 扣除经验 (可选)
    return ToggleResult.decremented;
  }
}
```

#### 4.4.2 打卡动效
- [ ] 打卡成功: 勾选动画 + 涟漪效果
- [ ] 完成目标: 庆祝动画 + 经验值飘字
- [ ] 可选: 震动反馈 + 音效

---

### 4.5 宠物养成系统 (Day 6-7)

#### 4.5.1 经验值服务
- [ ] **GamificationService**
```dart
class GamificationService {
  static const expPerGoal = 10;
  static const expPerDailyLogin = 5;
  
  int expNeeded(int level) => level * 100;
  
  Future<LevelUpResult?> awardExp(int exp) async {
    final state = await petRepo.getState();
    var newExp = state.exp + exp;
    var level = state.level;
    LevelUpResult? levelUp;
    
    while (newExp >= expNeeded(level)) {
      newExp -= expNeeded(level);
      level++;
      levelUp = LevelUpResult(newLevel: level);
    }
    
    await petRepo.updateState(level: level, exp: newExp);
    return levelUp;
  }
}
```

#### 4.5.2 宠物详情页
- [ ] **PetDetailPage**
  - 宠物大图/动画
  - 名字 (可编辑)
  - 等级和经验详情
  - 陪伴天数里程碑
  - 皮肤/形态展示 (预留)
  - 解锁条件说明

---

### 4.6 本地通知服务 (Day 7-8)

#### 4.6.1 通知服务实现
- [ ] **NotificationServiceImpl**
```dart
class NotificationServiceImpl implements NotificationService {
  final FlutterLocalNotificationsPlugin _plugin;
  
  Future<void> initialize() async {
    const androidSettings = AndroidInitializationSettings('@mipmap/ic_launcher');
    const iosSettings = DarwinInitializationSettings(
      requestAlertPermission: false,
      requestBadgePermission: false,
      requestSoundPermission: false,
    );
    
    await _plugin.initialize(
      InitializationSettings(android: androidSettings, iOS: iosSettings),
      onDidReceiveNotificationResponse: _onNotificationTap,
    );
  }
  
  Future<void> scheduleGoalReminder({
    required int goalId,
    required String goalName,
    required TimeOfDay time,
  }) async {
    await _plugin.zonedSchedule(
      goalId,  // 使用goalId作为通知ID
      '目标提醒',
      '别忘了完成今天的目标: $goalName',
      _nextInstanceOfTime(time),
      NotificationDetails(...),
      androidScheduleMode: AndroidScheduleMode.exactAllowWhileIdle,
      uiLocalNotificationDateInterpretation:
          UILocalNotificationDateInterpretation.absoluteTime,
      matchDateTimeComponents: DateTimeComponents.time,  // 每天重复
    );
  }
  
  void _onNotificationTap(NotificationResponse response) {
    // 跳转到目标页面
    router.go('/goals');
  }
}
```

#### 4.6.2 权限处理
- [ ] iOS: 首次启动请求通知权限
- [ ] Android 13+: 运行时请求 POST_NOTIFICATIONS 权限

---

### 4.7 目标回顾页 (Day 9-10)

#### 4.7.1 目标回顾页面
- [ ] **GoalReviewPage**
  - 顶部日期横向滚动选择器
  - 当前日期居中
  - 滑动选择查看历史日期
  
- [ ] 当日目标列表
  - 每个目标的完成状态
  - 完成次数 / 目标次数
  - 完成的打勾，未完成的灰色

#### 4.7.2 回顾数据用例
- [ ] **GetGoalReviewUseCase**
```dart
Future<GoalReview> call(DateTime date) async {
  final goals = await goalRepo.getAllGoals();
  final logs = await goalLogRepo.getLogsForDate(date);
  
  return GoalReview(
    date: date,
    items: goals.map((g) {
      final log = logs.firstWhere((l) => l.goalId == g.id, orElse: () => null);
      return GoalReviewItem(
        goal: g,
        progress: log?.progress ?? 0,
        isCompleted: (log?.progress ?? 0) >= g.target,
      );
    }).toList(),
  );
}
```

---

### Phase 4 验收标准
- [ ] 目标Tab显示宠物头部
- [ ] 可以查看和编辑宠物详情
- [ ] 可以从模板创建目标
- [ ] 可以创建自定义目标
- [ ] 打卡功能正常，经验值增长
- [ ] 宠物升级正常
- [ ] 目标提醒通知正常发送
- [ ] 目标回顾页可查看历史

---

## Phase 5: 回顾功能与AI聊天 (5-7天)

### 5.1 每周回顾计算 (Day 1-2)

#### 5.1.1 周回顾用例
- [ ] **GenerateWeeklySummaryUseCase**
```dart
Future<WeeklySummary> call(DateTime weekStart) async {
  final weekEnd = weekStart.add(Duration(days: 6));
  
  // 获取这一周的指标
  final metrics = await metricsRepo.getMetrics(weekStart, weekEnd);
  
  // 获取目标完成情况
  final goalLogs = await goalLogRepo.getLogsInRange(weekStart, weekEnd);
  final goals = await goalRepo.getAllGoals();
  
  // 计算统计数据
  final avgSleep = metrics.map((m) => m.sleepHours).average;
  final avgHrv = metrics.map((m) => m.hrv).whereNotNull().average;
  final totalSteps = metrics.map((m) => m.steps).sum;
  final totalCalories = metrics.map((m) => m.caloriesBurned).sum;
  final exerciseMinutes = metrics.map((m) => m.activeMinutes).sum;
  
  // 计算饮水天数 (假设有"喝水"目标)
  final waterDays = _calculateWaterDays(goalLogs, goals);
  
  // 计算目标完成率
  final completionRate = _calculateCompletionRate(goalLogs, goals);
  
  // 生成周称号
  final title = _generateTitle(avgSleep, totalSteps, exerciseMinutes, completionRate);
  
  final summary = WeeklySummary(
    weekStart: weekStart,
    avgSleepHours: avgSleep,
    avgHrv: avgHrv?.toInt(),
    totalSteps: totalSteps,
    totalCaloriesBurned: totalCalories,
    exerciseMinutes: exerciseMinutes,
    waterDays: waterDays,
    goalCompletionRate: completionRate,
    title: title,
  );
  
  // 缓存到数据库
  await summaryRepo.upsertSummary(summary);
  
  return summary;
}
```

#### 5.1.2 周称号生成规则
```dart
String _generateTitle(double avgSleep, int steps, int minutes, int rate) {
  // 综合评分
  final sleepScore = avgSleep >= 7 ? 1 : 0;
  final stepScore = steps >= 50000 ? 1 : 0;  // 周步数5万
  final exerciseScore = minutes >= 150 ? 1 : 0;  // 周运动150分钟
  final goalScore = rate >= 80 ? 1 : 0;  // 目标完成率80%
  
  final totalScore = sleepScore + stepScore + exerciseScore + goalScore;
  
  if (totalScore >= 4) return '能量满满';
  if (totalScore >= 3) return '运动达人';
  if (totalScore >= 2) return '还不错';
  if (totalScore >= 1) return '有待加油';
  return '压根没动';
}
```

---

### 5.2 周回顾页面 (Day 2-3)

#### 5.2.1 周回顾页面组件
- [ ] **WeeklyReviewPage**
  - 周称号大标题 (居中显示)
  - 周日期范围: "11月2日 - 11月8日"
  - 周切换箭头
  
- [ ] 指标卡片网格 (2列)
  - 平均睡眠: "7.2 小时"
  - 平均HRV: "45 ms"
  - 总步数: "52,340 步"
  - 饮水天数: "5 天"
  - 总消耗: "12,500 千卡"
  - 锻炼时长: "180 分钟"

- [ ] 底部分享按钮

---

### 5.3 分享卡片生成 (Day 3-4)

#### 5.3.1 分享服务
- [ ] **ShareCardService**
```dart
class ShareCardService {
  Future<Uint8List> generateWeeklyCard(WeeklySummary summary) async {
    // 创建卡片Widget
    final cardWidget = WeeklyShareCard(summary: summary);
    
    // 渲染为图片
    final boundary = RepaintBoundary(
      key: _boundaryKey,
      child: cardWidget,
    );
    
    // ... 渲染逻辑
    
    final image = await boundary.toImage(pixelRatio: 3.0);
    final byteData = await image.toByteData(format: ui.ImageByteFormat.png);
    
    return byteData!.buffer.asUint8List();
  }
  
  Future<void> shareCard(Uint8List imageBytes) async {
    // 保存到临时文件
    final tempDir = await getTemporaryDirectory();
    final file = File('${tempDir.path}/weekly_review.png');
    await file.writeAsBytes(imageBytes);
    
    // 调用系统分享
    await Share.shareXFiles([XFile(file.path)], text: '我的每周健康回顾');
  }
}
```

#### 5.3.2 分享卡片UI
- [ ] **WeeklyShareCard** Widget
  - App Logo
  - 周称号
  - 日期范围
  - 核心指标展示
  - 底部二维码/App名称

---

### 5.4 AI聊天 - UI (Day 4-5)

#### 5.4.1 聊天页面
- [ ] **ChatPage**
  - 顶部提示栏: "内容由AI生成，仅供参考，不构成医疗建议"
  - 消息列表 (ListView)
  - 底部输入区域:
    - 文本输入框
    - 发送按钮

- [ ] **MessageBubble** 消息气泡
  - 用户消息: 右侧，深色背景
  - AI消息: 左侧，浅色背景
  - 支持 Markdown 渲染
  - 加载中状态: 打字机光标动画

#### 5.4.2 快捷操作
- [ ] **QuickActions** 快捷按钮组
  - "今日饮食分析"
  - "帮我定一个本周目标"
  - "睡眠建议"
  - "运动计划"
  - 横向滚动

---

### 5.5 AI聊天 - 服务对接 (Day 5-6)

#### 5.5.1 健康上下文构建
- [ ] **BuildHealthContextUseCase**
```dart
Future<HealthContext> call() async {
  final today = DateTime.now().dateOnly;
  final metrics = await metricsRepo.getMetrics(today);
  final meals = await mealRepo.getMeals(today);
  final goals = await goalRepo.getAllGoals();
  final goalLogs = await goalLogRepo.getLogsForDate(today);
  final profile = await profileRepo.getProfile();
  final plan = await planRepo.getActivePlan();
  
  return HealthContext(
    todaySteps: metrics?.steps ?? 0,
    todayCaloriesBurned: metrics?.caloriesBurned ?? 0,
    todayCaloriesIntake: meals.sum((m) => m.calories),
    sleepHours: metrics?.sleepHours ?? 0,
    goals: goals.map((g) {
      final log = goalLogs.firstWhere((l) => l.goalId == g.id);
      return GoalProgress(
        name: g.name,
        progress: log?.progress ?? 0,
        target: g.target,
        isCompleted: (log?.progress ?? 0) >= g.target,
      );
    }).toList(),
    currentWeight: profile?.weight,
    targetWeight: plan?.targetWeight,
  );
}
```

#### 5.5.2 AI服务实现
- [ ] **AiChatServiceImpl**
```dart
class AiChatServiceImpl implements AiChatService {
  final Dio _dio;
  
  @override
  Future<String> sendMessage({
    required String message,
    required HealthContext context,
    List<ChatMessage>? history,
  }) async {
    final systemPrompt = _buildSystemPrompt(context);
    
    final response = await _dio.post(
      '/chat/completions',
      data: {
        'model': 'gpt-4o-mini',  // 或其他模型
        'messages': [
          {'role': 'system', 'content': systemPrompt},
          ...?history?.map((m) => m.toJson()),
          {'role': 'user', 'content': message},
        ],
      },
    );
    
    return response.data['choices'][0]['message']['content'];
  }
  
  String _buildSystemPrompt(HealthContext ctx) => '''
你是一个可爱的健康助手 TATA，帮助用户养成健康习惯。

用户今日数据:
- 步数: ${ctx.todaySteps}
- 消耗热量: ${ctx.todayCaloriesBurned} kcal
- 摄入热量: ${ctx.todayCaloriesIntake} kcal
- 睡眠: ${ctx.sleepHours} 小时
- 目标完成: ${ctx.goals.where((g) => g.isCompleted).length}/${ctx.goals.length}

规则:
1. 给出积极、鼓励性的建议
2. 不提供任何医疗诊断或处方建议
3. 建议要具体、可执行
4. 语气温和、可爱
5. 回复简洁，不超过200字
''';
}
```

#### 5.5.3 流式响应 (可选)
- [ ] SSE 处理
- [ ] 打字机效果展示

---

### 5.6 聊天状态管理 (Day 6-7)

#### 5.6.1 聊天 Notifier
- [ ] **ChatNotifier**
```dart
@riverpod
class ChatNotifier extends _$ChatNotifier {
  @override
  ChatState build() => ChatState.initial();
  
  Future<void> sendMessage(String content) async {
    // 添加用户消息
    state = state.addMessage(ChatMessage.user(content));
    
    // 显示加载状态
    state = state.copyWith(isLoading: true);
    
    try {
      final context = await ref.read(buildHealthContextUseCaseProvider).call();
      final response = await ref.read(aiChatServiceProvider).sendMessage(
        message: content,
        context: context,
        history: state.messages,
      );
      
      // 添加AI回复
      state = state.addMessage(ChatMessage.assistant(response));
    } catch (e) {
      state = state.copyWith(error: e.toString());
    } finally {
      state = state.copyWith(isLoading: false);
    }
  }
  
  void useQuickAction(String action) {
    // 预设的快捷提问
    final prompts = {
      'diet_analysis': '请分析一下我今天的饮食情况，给我一些建议',
      'weekly_goal': '帮我制定一个适合我的本周健康目标',
      'sleep_advice': '根据我的睡眠情况，给我一些改善睡眠的建议',
      'exercise_plan': '请给我制定一个简单的运动计划',
    };
    
    sendMessage(prompts[action] ?? action);
  }
}
```

---

### Phase 5 验收标准
- [ ] 每周回顾可正确计算和显示
- [ ] 周称号根据数据动态生成
- [ ] 可以生成分享卡片图片
- [ ] 分享功能正常
- [ ] AI聊天界面正常显示
- [ ] 可以发送消息并收到回复
- [ ] 快捷操作按钮正常工作

---

## Phase 6: 设置与优化发布 (5-7天)

### 6.1 设置Tab (Day 1-2)

#### 6.1.1 活力指数热力图
- [ ] **ActivityHeatmap**
  - X轴: 月份 (最近6个月)
  - Y轴: 周几 (周日-周六)
  - 每个格子代表一天的活动量
  - 颜色深浅表示活动等级

#### 6.1.2 设置项列表
- [ ] **SettingsPage**
  - 使用 ListView + ListTile 结构
  
- [ ] 设置项:
  - **关于你** → ProfilePage
  - **会员/专业版** → 预留 (灰色)
  - **数据来源** → HealthSourcePage
  - **语言** → 中文/English 选择
  - **通知** → 开关
  - **单位** → kg/lb, cm/inch 选择
  - **好友** → 预留 (灰色)
  - **云同步** → 预留 (灰色)
  - **更新日志** → 版本历史页
  - **常见问题** → FAQ 页面
  - **问题反馈** → 邮件/表单
  - **隐私协议** → WebView 或本地页面
  - **用户协议** → WebView 或本地页面

#### 6.1.3 设置持久化
- [ ] 使用 AppSettings 表存储
- [ ] 设置变更实时生效
- [ ] 单位切换影响全局显示

---

### 6.2 性能优化 (Day 3-4)

#### 6.2.1 数据库优化
- [ ] 添加索引
```dart
@TableIndex(name: 'idx_daily_metrics_date', columns: {#date})
@TableIndex(name: 'idx_goal_logs_date', columns: {#date})
@TableIndex(name: 'idx_goal_logs_goal_date', columns: {#goalId, #date})
```
- [ ] 优化查询 (避免N+1)
- [ ] 大数据量分页加载

#### 6.2.2 UI优化
- [ ] ListView.builder 使用 (避免全量渲染)
- [ ] 图片懒加载
- [ ] 动画性能检查
- [ ] const Widget 使用

#### 6.2.3 启动优化
- [ ] 懒加载非关键模块
- [ ] 数据库异步初始化
- [ ] Splash Screen 配置

#### 6.2.4 内存优化
- [ ] 图片缓存策略
- [ ] Provider 生命周期管理
- [ ] dispose 资源释放

---

### 6.3 测试 (Day 4-5)

#### 6.3.1 单元测试
- [ ] **计算逻辑测试**
  - BMI 计算
  - BMR/TDEE 计算
  - 热量预算计算
  - 经验值/升级计算

- [ ] **Repository 测试**
  - Mock 数据库
  - CRUD 操作测试

- [ ] **UseCase 测试**
  - 业务逻辑测试
  - 边界条件测试

#### 6.3.2 Widget 测试
- [ ] 核心组件渲染测试
- [ ] 表单验证测试
- [ ] 交互行为测试

#### 6.3.3 集成测试
- [ ] 关键用户流程
  - Onboarding 流程
  - 创建计划流程
  - 目标打卡流程
  - 健康数据同步流程

---

### 6.4 发布准备 (Day 5-7)

#### 6.4.1 iOS 发布
- [ ] App Store Connect 配置
  - App 名称和描述
  - 关键词
  - 截图 (6.7" & 5.5")
  - 预览视频 (可选)
  
- [ ] HealthKit 权限审核
  - 详细的权限使用说明
  - 隐私政策 URL
  
- [ ] TestFlight 内部测试
- [ ] 提交审核

#### 6.4.2 Android 发布
- [ ] Google Play Console 配置
  - 应用详情
  - 截图和图形
  - 内容分级

- [ ] Health Connect 权限
  - 权限声明审核
  - 数据使用披露

- [ ] 内部测试 / 公开测试 / 正式发布

#### 6.4.3 通用准备
- [ ] 隐私政策页面
- [ ] 用户协议页面
- [ ] App 图标和启动图
- [ ] 版本号管理

#### 6.4.4 错误监控
- [ ] Sentry 项目配置
- [ ] DSN 配置
- [ ] 崩溃上报测试
- [ ] 性能监控启用

---

### Phase 6 验收标准
- [ ] 设置Tab所有功能正常
- [ ] 单位切换全局生效
- [ ] 性能无明显卡顿
- [ ] 所有单元测试通过
- [ ] 集成测试通过
- [ ] iOS TestFlight 可安装运行
- [ ] Android 内测版可安装运行
- [ ] Sentry 错误上报正常

---

## 附录: 开发规范

### 代码风格
- 遵循 Dart 官方风格指南
- 使用 flutter_lints 规则
- 文件名: snake_case
- 类名: PascalCase
- 变量/方法名: camelCase

### Git 提交规范
```
feat: 新功能
fix: Bug修复
docs: 文档更新
style: 代码格式
refactor: 重构
perf: 性能优化
test: 测试相关
chore: 构建/工具
```

### 分支管理
- `main`: 生产分支
- `develop`: 开发分支
- `feature/*`: 功能分支
- `bugfix/*`: 修复分支
- `release/*`: 发布分支

### 代码审查要点
- 功能完整性
- 代码可读性
- 性能考虑
- 安全性检查
- 测试覆盖
